#include <stdbool.h>
// may change input
extern int menu();

extern int printMenuOptions();

// may change
extern int initializeMatrices();

extern float addOrSubtraction();

extern float multiplication();

extern bool sizeValidation(int option);

extern int randomMatrices(int size);

extern int printMatrix(float (*given)[100]);